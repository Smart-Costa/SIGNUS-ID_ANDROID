<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@color/blanco"
    tools:context=".UI.tomasfisicas.RegistroInventarioElectronicosActivity">

    <!-- 📌 Panel Superior -->
    <LinearLayout
        android:id="@+id/contenedorSuperior"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <TextView
            android:id="@+id/txtTitulo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Inventario de Electrónicos"
            android:textColor="#606060"
            android:textSize="22sp"
            android:textStyle="bold"
            android:layout_marginBottom="12dp" />

        <!-- 📌 Botones -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal">

            <Button
                android:id="@+id/btnTomas"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:background="@drawable/btn_primary"
                android:text="Tomas"
                android:textColor="@android:color/white" />

            <Button
                android:id="@+id/btnTomasCompletas"
                android:layout_width="0dp"
                android:layout_height="48dp"
                android:layout_weight="1"
                android:layout_marginStart="6dp"
                android:background="@drawable/btn_secondary_gray"
                android:text="Tomas Completas"
                android:textColor="@color/blanco" />
        </LinearLayout>

        <!-- 📌 Cabecera -->
        <LinearLayout
            android:id="@+id/lyCabecera"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:padding="8dp"
            android:layout_marginTop="18dp">

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Toma"
                android:textStyle="bold" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:text="Fecha"
                android:textStyle="bold" />

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:gravity="center"
                android:text="Estado"
                android:textStyle="bold" />
        </LinearLayout>

    </LinearLayout>

    <!-- 📌 Recycler -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerTomas"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/contenedorSuperior"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toTopOf="@id/kpiContainer" />

    <!-- 📌 KPI → AHORA ESTÁ DEBAJO DEL RECYCLER Y ANTES DEL PAGINADOR -->
    <FrameLayout
        android:id="@+id/kpiContainer"
        android:layout_width="140dp"
        android:layout_height="140dp"
        android:layout_marginTop="250dp"
        app:layout_constraintTop_toBottomOf="@id/recyclerTomas"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <ProgressBar
            android:id="@+id/kpiProgress"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:max="100"
            android:indeterminate="false"
            android:progress="60"
            android:rotation="-90"
            android:progressDrawable="@drawable/kpi_progressbar" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:orientation="vertical"
            android:padding="8dp">

            <TextView
                android:id="@+id/kpiValor"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="0"
                android:textSize="40sp"
                android:textStyle="bold"
                android:textColor="#333" />

            <TextView
                android:id="@+id/kpiTotal"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="de 5"
                android:textSize="16sp"
                android:textColor="#666" />
        </LinearLayout>
    </FrameLayout>

    <!-- 📌 Paginador -->
    <LinearLayout
        android:id="@+id/lyPaginador"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:paddingTop="12dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <ToggleButton
            android:id="@+id/btnNuevaToma"
            android:layout_width="160dp"
            android:layout_height="48dp"
            android:textOn="Crear Toma"
            android:textOff="Nueva Toma"
            android:background="@drawable/btn_toggle"
            android:textColor="@android:color/white"
            android:textStyle="bold"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
