<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".UI.main.MainActivity">

    <!-- Contenido principal -->
    <FrameLayout
        android:id="@+id/content_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Fondo opcional con logo -->
        <ImageView
            android:id="@+id/logo_fondo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:layout_marginTop="320dp"
            android:padding="36dp"
            android:contentDescription="@string/app_name"
            android:src="@drawable/logo2" />

        <!-- Contenido con AppBar -->
        <include
            android:id="@+id/appBarMain"
            layout="@layout/app_bar_main"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />
    </FrameLayout>

    <!-- Drawer correcto -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/nav_view"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        android:background="@color/nav_drawer_bg"
        android:divider="@null"
        app:headerLayout="@layout/nav_header_main"
        app:itemIconTint="@color/nav_item_icon_tint"
        app:itemRippleColor="@color/nav_item_ripple"
        app:itemShapeAppearanceOverlay="@style/NavDrawerItemShape"
        app:itemShapeFillColor="@color/nav_item_bg_tint"
        app:itemTextColor="@color/nav_item_text_tint"
        app:menu="@menu/activity_main_drawer" />

</androidx.drawerlayout.widget.DrawerLayout>
